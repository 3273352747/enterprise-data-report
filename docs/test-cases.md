# 企业经营数据分析与报表平台 - 功能测试报告

## 1. 测试信息

- 测试日期：2026-07-29
- 测试环境：Windows、Chromium 151、Node.js、Vite 本地开发服务器
- 测试方式：真实浏览器功能测试，结合页面文本、图表截图、控制台和导出文件内容核对
- 测试范围：Excel 导入、数据校验、经营分析、筛选、图表、报表导出、本地持久化和导入历史
- 执行结果：共执行 11 个用例，通过 11 个，失败 0 个，通过率 100%

## 2. 测试数据

- `public/经营数据模板.xlsx`：7 条合法经营数据
- `public/经营数据模板（错误数据）.xlsx`：7 条数据，其中 1 条营业收入不合法
- 缺表头测试文件：将“月份”表头改为“日期”
- 文件类型测试文件：与模板字段相同的 CSV 文件

重复导入测试使用了内容相同、文件名不同的副本，确保浏览器能够触发文件输入框的 `change` 事件。

## 3. 结果汇总

| 结果 | 数量 |
| --- | ---: |
| 通过 | 11 |
| 失败 | 0 |
| 阻塞 | 0 |
| 合计 | 11 |

## 4. 详细测试用例

### TC-01 正常导入并生成经营分析

- 优先级：P0
- 前置条件：页面处于无数据状态
- 操作步骤：上传 `经营数据模板.xlsx`，检查预览、校验结果、经营指标和图表
- 预期结果：成功解析 7 条数据，全部通过校验，并生成经营分析
- 实际结果：预览 7 条；有效 7 条、错误 0 条；营业收入 1,078,000 元、营业成本 730,000 元、利润 348,000 元、预算完成率 104.2%；折线图、柱状图和环形图均正常显示
- 测试结果：**通过**

### TC-02 拒绝非 xlsx 文件

- 优先级：P1
- 前置条件：页面处于无数据状态
- 操作步骤：上传 CSV 文件
- 预期结果：提示文件类型错误，不解析数据，也不写入导入历史
- 实际结果：提示“只支持 .xlsx 文件”；数据预览仍为 0 条；导入历史未新增该文件
- 测试结果：**通过**

### TC-03 缺少必填表头

- 优先级：P0
- 前置条件：Excel 文件中缺少“月份”表头
- 操作步骤：上传缺表头文件，随后重新上传合法文件
- 预期结果：明确指出缺少的表头；失败文件不进入预览；失败后仍可重新上传
- 实际结果：提示“Excel缺少必填列：月份。请使用下载的模板”；预览保持 0 条；随后上传合法文件可成功解析 7 条数据
- 测试结果：**通过**

### TC-04 行级数据校验

- 优先级：P0
- 前置条件：使用 `经营数据模板（错误数据）.xlsx`
- 操作步骤：上传文件并检查校验统计及错误明细
- 预期结果：合法行继续保留，错误行显示 Excel 行号和具体原因
- 实际结果：总数据 7 条、有效 6 条、错误 1 条；定位到 Excel 第 5 行，错误原因为“营业收入必须是非负数字”
- 测试结果：**通过**

### TC-05 导出错误数据清单

- 优先级：P1
- 前置条件：当前数据包含校验错误
- 操作步骤：点击“导出错误清单”，解析页面生成的 Excel 文件
- 预期结果：导出文件包含原始字段、Excel 行号和错误原因
- 实际结果：提示“错误数据清单导出成功”；生成 17,037 字节的 Excel 文件；工作表名为“错误数据”，范围为 `A1:I2`，包含第 5 行原始数据及对应错误原因
- 测试结果：**通过**

### TC-06 组合筛选与重置

- 优先级：P0
- 前置条件：已导入 7 条合法数据
- 操作步骤：先筛选月份 `2026-02`，再叠加部门“销售部”，最后点击“重置筛选”
- 预期结果：数据数量、指标和图表随筛选联动；重置后恢复全部数据
- 实际结果：筛选 `2026-02` 后剩余 3 条，收入 479,000 元、成本 326,000 元、利润 153,000 元；叠加“销售部”后剩余 1 条，收入 192,000 元、成本 131,000 元、利润 61,000 元；重置后恢复 7 条及全部指标
- 测试结果：**通过**

### TC-07 导出经营分析报表

- 优先级：P0
- 前置条件：当前存在合法经营数据
- 操作步骤：点击“导出分析报表”，解析页面生成的 Excel 文件
- 预期结果：导出汇总和明细两个工作表，数据与当前筛选结果一致
- 实际结果：提示“经营分析报表导出成功”；生成 20,929 字节的 Excel 文件；包含“分析汇总”和“经营明细”两个工作表，范围分别为 `A1:B9` 和 `A1:J8`；明细表包含 7 条数据及利润、预算完成率字段
- 测试结果：**通过**

### TC-08 刷新后恢复当前数据

- 优先级：P0
- 前置条件：已成功导入合法数据
- 操作步骤：刷新浏览器页面
- 预期结果：恢复文件名、数据预览、校验结果、分析指标和导入历史
- 实际结果：刷新后仍显示已导入文件；预览 7 条、有效 7 条、错误 0 条；经营指标和历史记录均恢复
- 测试结果：**通过**

### TC-09 清除当前数据

- 优先级：P1
- 前置条件：当前存在已导入数据和历史记录
- 操作步骤：点击“清除当前数据”
- 预期结果：清空当前预览和分析，不删除导入历史
- 实际结果：数据预览变为 0 条，经营分析区域不再展示；原导入历史记录仍保留
- 测试结果：**通过**

### TC-10 导入历史记录与清空

- 优先级：P1
- 前置条件：先后导入合法文件和错误数据文件
- 操作步骤：检查历史记录，再点击“清空历史”
- 预期结果：历史记录显示文件名、导入时间、总条数和校验结果；清空历史不影响当前数据
- 实际结果：合法文件显示“有效 7、错误 0”，错误文件显示“有效 6、错误 1”；清空后显示“暂无导入历史”，当前错误数据及校验结果仍保留
- 测试结果：**通过**

### TC-11 图表渲染与运行稳定性

- 优先级：P1
- 前置条件：已导入合法数据并完成筛选操作
- 操作步骤：检查三个图表，完成导入、筛选、导出、刷新和清空流程后查看浏览器错误及控制台
- 预期结果：图表非空、标签清晰，测试过程中无未捕获异常
- 实际结果：月度收入与成本折线图、部门利润柱状图、区域营收环形图均正常渲染；浏览器错误列表为空，控制台只有 Vite 连接调试信息
- 测试结果：**通过**

## 5. 测试结论

当前版本的核心业务流程完整可用，正常导入、错误拦截、分析联动、报表导出和本地持久化均达到预期，可以作为项目展示版本。

后续可继续补充以下边界测试：重复数据、全部数据均无效、损坏的 xlsx 文件、历史记录达到 8 条上限、浏览器本地存储不可用以及移动端布局。

现有“错误数据”模板实际只触发 1 条错误。为了更充分展示测试能力，建议后续增加一份综合异常数据文件，分别覆盖月份格式错误、必填字段为空、负数、预算收入为 0 和重复记录。
